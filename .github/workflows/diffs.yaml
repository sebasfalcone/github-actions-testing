on: pull_request
name: CI
jobs:
  diff:
    name: diff
    runs-on: ubuntu-latest

    strategy:
      matrix:
        project:
          - dependencies: 'src.*one.*cpp|src.*two.*cpp'
            path: src/one
          - dependencies: 'src.*two.*cpp'
            path: src/two
    steps:
      # Prepare project repository.
      - name: Checkout repository
        uses: actions/checkout@v3
      
      - name: Check for compilation requirement
        id: test
        run: |
          #DIFFS=$(git diff --name-only HEAD ${{ github.base_ref }})
          DIFFS=$(gh pr diff ${{ github.event.pull_request.number }} --name-only)
          echo $DIFFS
